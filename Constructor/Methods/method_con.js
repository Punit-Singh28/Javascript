// Created a Object

let user = {
    name: 'Punit'
}
console.log('before user Manipulation: ',user);

function Detail(name, age, skill){
    this.name = name;
    this.age = age;
    this.skill = skill;
    return this
}

// let user_val = Detail.call(user, 'Pratigya', 100, 'KGL ki maarna');
// console.log('After user Manipulation: ',user);

let 

